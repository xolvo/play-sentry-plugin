# Application dependencies

require:
    - play
    - ru.purecode -> sentry 0.3

repositories:
    - purecode:
        type: local
        artifact: "${application.path}/../sentry/dist/[module]-[revision].zip"
        contains:
            - ru.purecode -> *
